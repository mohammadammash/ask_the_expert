import { StyleSheet } from "react-native";

const expertStyles = StyleSheet.create({
    starsRatingRow: { display: "flex", alignItems: "center", height: 80 },
    height_10px: { height: 10 }
});

export default expertStyles;